{
  "name": "@pantry/engine",
  "version": "0.1.0",
  "private": true,
  "description": "Constraint builder and recipe engine (SPEC sections 7 and 16). Pure logic: no env, no network.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -p ."
  },
  "dependencies": {
    "@pantry/contract": "*",
    "@pantry/vocabulary": "*",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1"
  }
}
