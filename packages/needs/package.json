{
  "name": "@pantry/needs",
  "version": "0.1.0",
  "private": true,
  "description": "Needs calculator: body stats to daily calorie and protein targets (SPEC section 8).",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -p ."
  },
  "dependencies": {
    "@pantry/contract": "*",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vitest": "^5.0.1"
  }
}
