{
  "name": "@pantry/backend",
  "version": "0.1.0",
  "private": true,
  "description": "Server side: config, model and image adapters. The only package that reads env or holds keys.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -p .",
    "deal": "tsx --env-file-if-exists=../../.env src/deal.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.128.0",
    "@pantry/contract": "*",
    "@pantry/engine": "*",
    "@pantry/vocabulary": "*",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.23.15",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1"
  }
}
