{
  "name": "@pantry/vocabulary",
  "version": "0.1.0",
  "private": true,
  "description": "Families, groups and items (SPEC section 6). Data in data/, refreshed monthly without code changes.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -p ."
  },
  "dependencies": {
    "@pantry/contract": "*",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vitest": "^5.0.1"
  }
}
